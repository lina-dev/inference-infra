# AWS CDK app dependencies (Python).
# Install into a virtualenv:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
aws-cdk-lib>=2.170.0,<3.0.0
constructs>=10.0.0,<11.0.0
# Pinned kubectl Lambda layer matching the cluster's Kubernetes version (1.31).
aws-cdk.lambda-layer-kubectl-v31>=2.0.0
